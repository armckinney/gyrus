TODO:
- testing:
  - integration 
    - plugins (test that these agents properly leverage the plugin tools e2e)
      - agy cli
      - copilot cli
    - all providers with actual infrastructure backing it
      - storage
      - index
      - search
      - graph
  - installation (test that these can be installed from recommended distribution)
    - cli
    - plugins
      - agy cli (custom plugin format for now)
      - copilot & codex cli (agent-plugins standard)
      - claude code (needed but cannot validate claude code)
    - MCP container
  - mcp
    - serving from cli
    - mcp container serving functions properly
  - setup
    - config
    - plugins (should we offer simple subcommands for that can install plugins on user's behalf?)
