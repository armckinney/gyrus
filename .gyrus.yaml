version: 1
profile: local_full
schemas_path: ./.gyrus/schemas

storage:
  provider: localfs
  root: ./.gyrus/docs

index:
  provider: sqlite
  dsn: ./.gyrus/docs/index.db

graph:
  provider: sqlite

search:
  provider: sqlite_fts5
