version: 2
project_name: gyrus

before:
  hooks:
    - go mod tidy
    - bash -c 'TAG="${GORELEASER_CURRENT_TAG:-latest}"; tar -czf gyrus-plugin.tar.gz -C packaging/plugins gyrus && cp gyrus-plugin.tar.gz "gyrus-plugin_${TAG}.tar.gz"'

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64
    main: ./cmd/gyrus
    binary: gyrus
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.version={{.Version}}

archives:
  - formats:
      - tar.gz
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        formats:
          - zip

checksum:
  name_template: "checksums.txt"

snapshot:
  version_template: "{{ .Tag }}-next"

release:
  extra_files:
    - glob: ./gyrus-plugin*.tar.gz

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
